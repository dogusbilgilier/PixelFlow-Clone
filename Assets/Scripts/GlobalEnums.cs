public enum GameColor
{
    Yellow,
    Orange,
    Green,
    Blue,
}

public enum GameplayState
{
    Menu,
    Gameplay,
    GameplayFinished
}

public enum EditTool
{
    Paint,
    Remove
}

public enum GridType
{
    Shooter,
    Target
}

public enum Side
{
    Bottom,
    Right,
    Top,
    Left
}